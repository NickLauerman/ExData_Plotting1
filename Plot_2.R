# Plot 2
# Time series of Global Active Power 
# 
file = "household_power_consumption.txt"
data <- read.table(file = file,
                   header = TRUE,
                   sep = ";",
                   na.strings = "?",
                   stringsAsFactors = FALSE)


#wrap in code to produce the png
paste(Date, Time)
as.POSIXlt(z, format = "%d/%m/%Y %H:%M:%S")